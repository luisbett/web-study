//This is an example of an undefined variable
let value
console.log(value)

//This is an example os null variable
value = null
console.log(value)

/*
* I set null for a variable when I want that it doesn't refer to a memory adress anymore
*/

//I can't do it because it causes an error
//console.log(value.toString())

//It is undefinied as well
const product = {}
console.log(product.price)