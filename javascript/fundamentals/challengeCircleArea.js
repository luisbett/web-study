/*
    You have to find the circle area using the formula below
    PI * radius * radius
*/

//I can use the PI value from the math object as well
const PI = Math.PI

let radius = 10
let areaCircle = PI /*or use Math.PI*/ * radius * radius

console.log("A circle with radius equal " + radius + " have the area equal: " + areaCircle + " m2")